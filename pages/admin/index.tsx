import { Loader } from "../../components/Loader";

export default function AdminPage({}) {
  return (
    <main className="tw_container">
      <h1>adminpage</h1>
      <Loader show />
    </main>
  );
}
